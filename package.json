{
  "name": "psy-method-app",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12 <23"
  },
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "deploy:build": "npm ci --include=dev && npm --prefix client ci --include=dev && npm --prefix server ci --include=dev && npm run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
